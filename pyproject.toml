[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "goldmarketsentinel"
version = "0.1.0"
description = "Macro intelligence and alerting system"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src"]